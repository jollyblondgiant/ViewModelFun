<h3>Here are some Users!</h3>
<div id="users">
    @using ViewModelFun.Controllers
    @model List<User>
    @{
        foreach(var user in @Model){
            <p>@user.FirstName @user.LastName</p>
        }
        
    }



</div>